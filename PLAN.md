# Masil 개발·학습 계획

상태: 2026-09-06 초안. 사용자 요청에 따라 더미 데이터로 앱을 먼저 개발하고, 이후 새 어드민·서버를 연결한다.

이 문서는 개발 단계·순서·학습 목표·완료 기준과 현재 진행 상태를 정리한다. 작업 방식과 보고 규칙은 [AGENTS.md](./AGENTS.md)를 따른다.

## 현재 상태

- Expo·React Native·Expo Router 기반의 기본 화면과 기기 실행 스크립트가 있다.
- 홈에 브랜드 소개·대표 글 이미지·글 목록을 적용했다. 글 ID로 진입하는 상세 화면에는 조회 결과를 표시하는 초기 코드가 있고, 없는 글 처리·상세 디자인·기기 동작 검증은 남아 있다. WebView 통신과 콘텐츠 서버 연결은 아직 구현하지 않았다.
- 초기 홈 목록은 Android 에뮬레이터에서 확인했고, 이후 홈 디자인은 사용자의 iOS 캡처로 검토했다. 최신 변경의 Android 검증과 글자 확대·접근성 검증은 별도로 이어간다.
- 홈 HTML 시안을 바탕으로 앱을 구현했으며, 상세는 [읽기 시안 01](./DESIGN.md#상세-읽기-시안-01)을 먼저 사용자와 검토한 뒤 앱에 적용한다. 임시 상세 스타일의 수치를 확정 디자인으로 삼지 않는다.
- 제품·디자인·작업 방식의 문서 초안이 있다. 앱 화면 디자인과 최종 출시 범위는 미정이다.
- 하단 탐색은 `(tabs)/_layout.tsx`의 JavaScript `Tabs`로 홈·서재를 구성했고, 루트 `Stack`에서 상세를 탭 바깥에 배치했다. 상세 진입·복귀 후 목록 위치 유지와 플랫폼별 동작 검증은 이어간다.
- 실제 콘텐츠 준비와 서버 기술 선택은 첫 앱 구현의 선행 조건이 아니다.

제품 판단은 [PRODUCT.md](./PRODUCT.md), 시각 기준은 [DESIGN.md](./DESIGN.md), 작업 지침은 [AGENTS.md](./AGENTS.md)를 따른다.
아래 구체적인 단계와 기능 구성은 작업 제안이며, 진행 중 배운 내용에 맞춰 조정한다.

## 단계별 계획

| 단계 | 만들거나 정할 것 | 학습 목표 | 완료 기준 | 상태 |
| --- | --- | --- | --- | --- |
| 0. 문서와 화면 방향 | 제품·디자인·작업 기준, Home·Article의 초기 구성 | 제품 요구와 기술 선택의 관계 | 확정·제안·미정이 구분된 문서와 첫 화면의 설계 근거가 있음 | Home 검토·앱 적용, Article 읽기 시안 작성·브라우저 검증, 사용자 검토 예정 |
| 1. 더미 데이터로 기본 흐름 | 글 목록 → 상세 화면 → 뒤로 가기, 초기 브랜드 적용 | 네이티브 컴포넌트·스타일·리스트·라우팅 | 같은 글 데이터가 목록과 상세에 연결되고 두 플랫폼에서 확인됨 | 진행 중: 홈 브랜드·공통 탭·상세 라우트 적용, 상세 시안 검토 후 스타일·조회 실패 처리와 양 플랫폼 검증 예정 |
| 2. WebView 읽기와 통신 | 읽기용 본문, 글자 크기 전달, 본문 준비·읽는 위치 메시지 | 네이티브와 웹의 실행 환경·메시지·상태 소유권 | 양방향 전달과 준비 시점 처리가 동작하고 실패 조건을 설명할 수 있음 | 예정 |
| 3. 저장과 다시 읽기 | 저장 목록, 읽기 설정 유지, 읽던 위치 복원 | 기기 저장·앱 수명주기·복원 | 앱 재실행 후 상태가 복원되고 글자 크기·본문 변경 시의 처리 규칙을 확인함 | 예정 |
| 4. 새 어드민·서버 연결 | 데이터 형식·접근 권한 결정, 에디터 작성·초안·발행, 앱 조회 | 실제 API·비동기 상태·운영 흐름 | 발행한 글이 두 플랫폼에 표시되고 초안과 쓰기 권한이 보호됨 | 예정 |
| 5. 실제 운영 검증 | 에디터 편의성, 실제 글, 오류·재시도·접근성 보완 | 더미 환경과 실제 환경의 차이 | 에디터가 직접 발행하고 독자가 핵심 흐름을 사용하며 발견된 주요 문제를 해결함 | 예정 |
| 6. 배포 | 테스트 배포, 스토어용 빌드와 등록, iOS·Android 출시 | 개발·테스트·출시 빌드와 배포 과정 | 두 스토어에 실제 출시하고 사용한 버전·검증·배포 결과를 기록함 | 예정 |

양 플랫폼 검증은 1단계부터 반복한다. 개발 초기부터 development build를 준비하고 Expo Go와의 차이를 경험한다.
더미 데이터로 핵심 읽기·통신·복원을 확인한 뒤 실제 데이터 연결로 넘어간다. 부가 기능을 계속 늘리며 서버 연결을 미루지 않는다.

## 커스텀 탭 적용 순서

1단계의 화면 구성·탐색 작업으로 진행한다. 목적·메뉴 구성은 [PRODUCT.md](./PRODUCT.md#하단-탐색-구성), 모양·상태·기기 확인 기준은 [DESIGN.md](./DESIGN.md#하단-탭-디자인)이 담당한다.

구현안은 Expo Router의 [JavaScript Tabs](https://docs.expo.dev/router/advanced/tabs/)를 기반으로 한다. 제공되는 스타일 옵션으로 시작하고, 디자인을 충족하기 어려운 부분에만 `tabBar` 컴포넌트를 연결한다. 두 플랫폼에서 공통 UI를 사용하면서 라우팅·선택 상태·탭 이벤트는 네비게이터를 활용한다. 초기부터 별도의 탐색 상태나 내비게이션 엔진을 만들지 않는다.

| 순서 | 작은 작업 단위 | 학습·완료 기준 |
| --- | --- | --- |
| 1 | 홈·서재 구성안과 하단 바 형태 검토 | 각 탭의 목적과 주요 화면에서 보일 상태를 설명할 수 있음 |
| 2 | 현재 홈을 JavaScript `Tabs`로 옮기고 서재 개발 화면 연결 | 기존 홈이 유지되고 두 목적지로 이동함. 서재가 아직 개발 중임을 구분함 |
| 3 | 공통 탭의 색상·아이콘·라벨·배치 적용 | 디자인 토큰과 선택 상태가 연결되고 양 플랫폼에서 안전 영역·글자 확대·터치·접근성을 확인함 |
| 4 | 상세 화면을 탭 바깥의 Stack 화면으로 연결 | 홈·서재에서 같은 글 ID로 진입하고 iOS 뒤로 가기·Android 시스템 뒤로 가기 후 원래 목록과 위치로 복귀함 |
| 5 | 3단계의 저장 기능과 서재 연결 | 실제 저장·해제 결과와 빈 상태를 구분하고 앱 재실행 후 유지됨. 저장 실패를 처리하고 미완성 화면을 출시하지 않음 |

각 행은 작업 상한이 아니라 목표이며, 변경 규모에 따라 더 작게 나눈다. 새로운 탭 방식에서는 기존 `NativeTabs`의 안전 영역 처리에 의존하던 코드와 주석도 함께 검토한다. 시각적 변경만으로 탐색·상태 보존·기기 검증까지 완료했다고 판단하지 않는다.

## 더미 데이터와 본문 형식

- 한곳에서 관리하는 고유 ID 기반의 예시 글을 사용한다.
- 짧은 글, 긴 제목과 본문, 이미지가 없는 글, 이미지·인용·링크가 있는 글을 준비한다.
- 본문은 WebView 학습을 위해 간단한 HTML로 시작하는 안을 제안한다. 최종 에디터 형식이나 DB 스키마를 확정하는 것은 아니다.
- 화면별로 같은 글을 따로 하드코딩하지 않는다. API 연결을 예상한 거대한 공통 계층은 만들지 않는다.
- 더미 데이터의 변경에 따라 저장된 글과 읽는 위치가 어떻게 처리되는지도 확인한다.
- 더미 데이터로 확인한 동작과 실제 API의 로딩·오류·재시도 검증을 구분한다.

## 네이티브와 WebView의 역할 제안

네이티브는 화면 이동·목록·서재·읽기 컨트롤·기기 저장을 맡고, WebView는 앱이 관리하는 읽기용 HTML 본문을 표시한다.
기존 웹사이트 전체를 앱 화면으로 사용하는 것을 기본 구성으로 삼지 않는다.

| 흐름 | 목적 |
| --- | --- |
| 네이티브 → WebView: 읽기 설정 | 글자 크기 등 본문 표시 설정 적용 |
| WebView → 네이티브: 준비 완료 | 설정 적용과 위치 복원의 시작 시점 결정 |
| WebView → 네이티브: 읽는 위치 | 위치 보관과 이어 읽기 |
| 네이티브 → WebView: 복원 요청 | 준비된 본문에서 저장한 위치 복원 |

메시지 이름과 데이터 구조는 구현 시 필요한 만큼 정한다.
메시지 형식 검증, WebView 재로드, 본문 준비 전 요청, 이미지 로딩·글자 크기 변경, 외부 링크 탐색을 확인한다.
네이티브·WebView·서버 각각의 역할을 설명하고, 화면 높이·스크롤·저장 상태의 담당을 명확히 한다.

## 어드민·서버 단계에서 결정할 것

- 새 프로젝트의 기술과 저장소 구성: Next.js·Tiptap·Supabase·별도 저장소는 후보다.
- 콘텐츠 편집 원본과 읽기용 본문의 형식, 공개 조회 범위, 이미지 저장 방식.
- 지정된 에디터의 인증·작성 권한과 초안 접근 규칙.
- 발행된 글의 수정·발행 취소가 독자 앱과 저장 목록에 반영되는 방식.
- 저장 성공·실패 표시, 미저장 내용 보호, 미리보기 등 에디터의 실제 작업 요구.

기존 실험 프로젝트의 코드·데이터 이관은 기본 범위에 포함하지 않는다.
