# Masil 앱 디자인 기준

상태: 2026-09-06 초안. 랜딩의 브랜드 기준을 추출했으며, 앱 화면 디자인은 아직 확정하지 않았다.

## 출처와 적용 범위

- 원본 프로젝트: `masil/apps/landing`.
- 원본 기준: `masil/apps/landing/DESIGN.md`.
- 시각·인터랙션 참고: `masil/apps/landing/docs/prototypes/masil-landing-v3.html`.
- 구현 색상: `masil/apps/landing/src/app/globals.css`, `masil/packages/ui/styles/base.css`.
- 참고한 랜딩 최종 변경: `b2c15a7` — 2026-08-31, landing 리디자인 코드 정리 및 최종 검증.

이 문서의 `masil/` 경로와 커밋은 별도 원본 저장소의 출처 표기이며, 해당 파일은 이 저장소에 포함되어 있지 않다.
아래에 핵심 기준을 기록해 원본 저장소 없이도 앱의 디자인 방향을 이해할 수 있게 한다.

원본의 멤버 5명·파트너 4곳·세미나 CTA·섹션 순서는 랜딩에 해당한다.
원본의 ‘디자인 탐색 완료’ 상태나 landing 전용 AGENTS.md를 앱 전체의 규칙으로 가져오지 않는다.
앱의 구체적인 화면 구성과 모바일 인터랙션은 이 제품의 읽기 경험에 맞게 설계한다.

## 읽기 경험과 구성 원칙

- 에디터가 발행한 글을 발견하고 선택해 본문에 집중할 수 있도록 구성한다.
- 화면의 정보 우선순위는 마실의 콘텐츠 양과 발행 방식에 맞춰 정한다.
- 글의 주제와 읽을 이유를 명료하게 소개하고, 편안하게 읽기를 권하는 말투를 검토한다.

제품 범위와 미정 사항은 [PRODUCT.md](./PRODUCT.md)를 따른다. 홈 배치는 시안에서, 탭 구성은 제품의 하단 탐색 구성안과 아래 디자인 초안에서 검토한다.

## 이어받을 시각적 성격

- 짙은 검정과 어두운 초록, 웜화이트 글자, 제한적인 마실 그린.
- 정돈된 타이포그래피와 넉넉한 여백, 차분하고 현대적인 인상.
- 일러스트와 클로버에서 느껴지는 사람의 온기와 작은 장난스러움.
- 강한 제목과 그래픽을 사용할 수 있지만 긴 글의 가독성을 우선한다.
- 정보와 행동을 돕는 절제된 모션·제스처·햅틱.

과도한 포스터 콜라주, 임의의 비대칭, 네온 효과, 반복적인 카드·그림자·장식으로 화면을 채우지 않는다.

## 색상

다음은 랜딩 구현의 기준값이다. HEX는 브라우저에서 sRGB로 변환한 근삿값이다. 기본 배경·기본 글자·보조 글자 색상은 앱의 어두운 테마에 반영했으며, 나머지 역할의 연결과 전체 기기 검증은 진행 중이다.

| 역할 | 원본 값 | HEX 참고 |
| --- | --- | --- |
| 기본 배경 | `oklch(0.14135 0.00581 145.26)` | `#080A08` |
| 보조 배경 | `oklch(0.16844 0.00782 145.22)` | `#0D100D` |
| 기본 글자 / 웜화이트 | `oklch(0.95475 0.00955 93.57)` | `#F2F0E9` |
| 보조 글자 | `oklch(0.73416 0.00718 97.39)` | `#AAA9A4` |
| 구분선 | `oklch(0.32595 0.01247 140.47)` | `#313630` |
| 마실 그린 | `oklch(0.75 0.15 145)` | `#6BC670` |

그린은 로고, 주요 행동, 선택·저장 상태 등에 제한적으로 사용한다.
넓은 초록 배경이나 밝은 네온 효과를 기본 표현으로 삼지 않는다.
초기 앱은 시스템의 라이트·다크 모드와 관계없이 마실의 어두운 스타일 하나로 통일한다. 홈·탭·네비게이션·상태 표시줄·앱 시작 화면에 일관되게 적용하며, 구현과 기기 검증은 단계적으로 진행한다. 읽기 화면의 밝은 테마 제공 여부는 실제 장문 콘텐츠로 읽기 경험을 확인한 뒤 별도로 검토한다.

## 타이포그래피

- 기본 서체: Pretendard.
- 브랜드 서체: 학교안심 물결체(Hakgyoansim Mulgyeol). 로고와 제한적인 ‘마실’ 표현에 사용한다.
- 제목은 주로 600, 필요할 때 700을 사용한다. 800~900을 기본 제목 굵기로 삼지 않는다.
- 본문은 400, 보조 강조와 라벨은 500~600을 기준으로 검토한다.
- 한국어 단어와 문장의 자연스러운 줄바꿈을 확인한다. 좁은 화면에 강제 줄바꿈을 과도하게 넣지 않는다.
- 랜딩의 제목 행간 약 1.15~1.3, 본문 약 1.5~1.7은 참고값이다. 앱의 본문 크기·행간·여백은 긴 글과 글자 확대 상태로 검증한다.

원본 글꼴은 `masil/packages/ui/src/fonts`에 있고, 웹 로더는 `masil/packages/ui/src/styles/fonts.ts`에 있다.
네이티브와 WebView에서 사용할 파일 형식·폰트 이름·굵기 연결은 실제 도입 시 각각 확인한다.
현재 앱에는 마실 서체를 아직 연결하지 않았다.

## 자산

- 클로버: `masil/apps/landing/public/images/redesign/brand/masil-clover-cutout.png`.
- 커뮤니티 일러스트: `masil/apps/landing/public/images/redesign/activity/img-community-cutout.png`.
- 인물·파트너 자산: 같은 `redesign` 폴더의 `people`, `partners` 하위 경로.

기존 브랜드 자산을 우선 사용하고 사람·로고·클로버를 임의로 다시 생성하거나 형태를 바꾸지 않는다.
클로버는 현재 홈 시안에서 제외한다. 다른 화면에서의 사용 여부는 미정이다.
인물·파트너 자산은 실제 해당 콘텐츠가 있을 때 사용한다. 일반 독자의 아바타로 임의 배정하지 않는다.
홈 시안에 사용하는 서체와 이미지 사본은 [시안 자산 폴더](./docs/prototypes/assets)에 있다. 서체 라이선스 고지도 함께 포함한다.
이 자산은 현재 HTML 시안에서만 사용하며, 네이티브 앱에는 아직 연결하지 않았다.

## 앱 적용 제안

| 영역 | 방향 |
| --- | --- |
| Home | 브랜드 문구와 콘텐츠가 함께 보이는 구성. 대표 글과 글 목록의 위계를 구분 |
| Article | 본문은 왼쪽 정렬, 안정적인 대비와 문단 간격. 본문 위로 움직이는 장식을 겹치지 않음 |
| Save / 서재 | 저장한 글을 다시 찾기 쉬운 목록 |
| Settings | 읽기 환경을 바꾸고 결과를 이해하기 쉬운 컨트롤 |

큰 브랜드 메시지에는 중앙 정렬을 활용할 수 있다. 목록과 상세 정보는 콘텐츠에 맞는 정렬을 선택한다.
카드는 정보 묶음이 필요할 때 사용하고, 여백·구분선·이미지로도 위계를 만든다.
화면 구성과 하단 탐색 구성안, 아이콘, 정확한 크기와 간격은 구현·기기 검토를 거쳐 확정한다.

### 하단 탭 디자인

상태: 공통 커스텀 탭 UI를 사용하는 방향은 확정했다. 메뉴별 목적은 [제품의 하단 탐색 구성](./PRODUCT.md#하단-탐색-구성)을 따른다. 아래 형태·치수·상태 표현은 홈·서재 두 탭을 기준으로 한 시각 초안이며, 앱 구현과 양 플랫폼 검증 전이다.

목표는 콘텐츠의 분위기를 이어받으면서 현재 위치와 다른 목적지를 쉽게 구분하게 하는 것이다. 화면 하단을 가로로 채우는 불투명한 바를 사용하고, 두 항목을 같은 너비로 배치한다. 캡슐·떠 있는 버튼·큰 그림자·블러는 초기 형태에 사용하지 않는다. 목록 콘텐츠와 탭 바는 레이아웃 공간을 나눠 가지며 탭을 콘텐츠 위에 겹쳐 놓지 않는다.

| 요소 | 초기 제안 |
| --- | --- |
| 배경 | 보조 배경 `#0D100D`, 하단 안전 영역까지 같은 색으로 연결 |
| 상단 경계 | 구분선 `#313630`, 기기에서 보이는 얇은 선 |
| 선택된 항목 | 아이콘·라벨에 마실 그린 `#6BC670` 적용, 채워진 아이콘으로도 선택 상태 구분 |
| 선택되지 않은 항목 | 아이콘·라벨에 보조 글자색 `#AAA9A4`, 외곽선 아이콘 |
| 아이콘 | 홈은 집, 서재는 북마크. 두 플랫폼에서 같은 도형·크기를 사용하고 클로버·로고는 넣지 않음 |
| 라벨 | “홈”, “서재”를 항상 표시. Pretendard 12 논리 단위·굵기 500을 출발점으로 검토하며, 서체 도입 전에는 현재 기본 서체 사용 |
| 아이콘 크기·간격 | 24 논리 단위, 아이콘 아래 라벨까지 4 논리 단위 |
| 바 높이 | 하단 안전 영역을 제외한 최소 높이 60 논리 단위. 글자 확대 시 필요한 높이로 늘어날 수 있게 구성 |
| 터치 영역 | 아이콘뿐 아니라 항목 전체를 누를 수 있게 하고 최소 48×48 논리 단위 확보 |

각 항목은 아이콘 위·라벨 아래의 세로 배치로 중앙 정렬한다. 색상은 화면에 반복해서 직접 적지 않고 의미가 있는 공통 색상 값으로 연결한다. 이 치수는 고정된 물리 픽셀값이나 검증된 접근성 결과가 아니다.

동작과 검증 기준:

- 홈과 서재의 최상위 화면에서 같은 위치에 표시한다. 상세 읽기 화면에서는 숨기고 뒤로 가기로 원래 목록에 복귀한다.
- 탭 선택은 라벨과 채워진 아이콘으로도 구분하고, 화면 읽기 도구에 이름·탭 역할·선택 상태를 전달한다. 보조 아이콘이 라벨과 중복 낭독되지 않게 한다.
- 누르는 동안 짧은 배경색 변화로 반응을 표시한다. 초기에는 튀는 애니메이션·자동 축소·추가 햅틱을 넣지 않는다.
- 탭을 바꿨다가 돌아왔을 때 목록 위치를 유지하는 것을 목표로 한다. 재선택 시 맨 위로 이동하는 추가 기능은 보류한다.
- 하단 안전 영역은 고정된 숫자로 추정하지 않는다. 네비게이터가 처리하는 여백과 사용자 스타일의 여백을 중복 적용하지 않고, iOS 홈 인디케이터와 Android 제스처·버튼 탐색 환경에서 확인한다.
- 글자 확대 시 라벨이 잘리거나 서로 겹치지 않는지, 마지막 글이 탭에 가려지지 않는지, 선택·비선택 상태 모두 읽히는지 확인한다. OS가 제공하는 시스템 영역까지 동일한 외형으로 만들지는 않는다.

기존 홈 시안 01에는 탭이 없으며, 아래 하단 탐색 시안에서 공통 바 형태를 검토한다. 서재가 미구현된 상태를 실제 빈 저장 목록처럼 표현하지 않는다.

#### 하단 탐색 시안 01

- [탭을 눌러 확인하는 HTML](./docs/prototypes/home-tabs-v1.html): 기존 홈 시안을 바탕으로 홈·서재 전환, 선택·비선택 아이콘, 서재 빈 상태의 홈 복귀를 확인한다. 홈 목록의 스크롤 위치는 탭 이동과 재선택 시 유지한다.
- [홈 선택 화면](./docs/prototypes/home-tabs-v1.png), [서재 선택 화면](./docs/prototypes/library-tabs-v1.png): 390×844 CSS 픽셀 화면을 2배 해상도로 캡처했다.
- 넓은 브라우저에서는 검토 안내와 휴대폰 형태의 화면을 함께 표시한다. 좁은 브라우저에서는 시안 화면만 표시하며, 내부 콘텐츠가 스크롤되고 하단 바는 자체 공간을 차지한다.
- 서재의 “빈 상태 시안” 표시는 예시임을 알리는 검토용 표시다. 실제 저장·상세 화면 이동은 연결하지 않았다. 라벨은 현재 시안 자산에 있는 Pretendard 600을 사용하며, 500 적용은 앱의 서체 도입 시 검토한다.
- 상태 표시줄과 하단 인디케이터·안전 영역은 시각적 표현용이다. 브라우저의 글자 확대 검사는 OS 글자 확대 및 실제 iOS·Android의 안전 영역·화면 읽기 도구 검증을 대신하지 않는다.

### 홈 화면 초기 구성

상태: 2026-09-07 구성 초안. 화면 구현과 기기 검증 전이며, 사용해보면서 조정한다.
홈의 목표는 독자가 글의 주제와 읽을 이유를 파악하고 한 편을 선택할 수 있게 하는 것이다.

상단부터 다음 순서로 배치하고, 하나의 세로 스크롤로 탐색한다.

| 순서 | 영역 | 표시할 정보와 역할 |
| --- | --- | --- |
| 1 | 브랜드 표시 | “일과 삶 사이, 잠깐 마실.” 문구와 짧은 소개. 타이틀의 ‘마실’이 브랜드 역할을 맡으며, 별도 상단 로고나 클로버 없이 구성 |
| 2 | 먼저 읽어볼 이야기 | 대표 글 한 편의 제목, 짧은 소개, 에디터 이름, 선택적인 이미지. 첫 방문자가 읽기를 시작할 지점을 제공 |
| 3 | 더 읽어볼 이야기 | 나머지 글의 제목, 짧은 소개, 에디터 이름, 선택적인 썸네일. 다른 글을 비교하고 선택 |

- 초기 더미 데이터에서는 첫 글을 대표 글로 표시하고, 나머지를 데이터 순서대로 나열한다. 대표 글을 아래 목록에 중복 표시하지 않는다. 실제 발행 후의 선정·정렬 방식은 운영 요구에 맞춰 정한다.
- 대표 글은 큰 제목과 넉넉한 여백으로 강조하고, 이미지가 있으면 제목 위에 배치한다. 목록은 텍스트를 왼쪽에, 선택적인 작은 썸네일을 오른쪽에 두고 구분선으로 나눈다.
- 상단 소개는 짧게 유지해 읽을거리가 일찍 보이게 한다. 기존 색상·서체 기준을 적용하고, 긴 제목과 글자 확대에 따라 영역 높이가 늘어나도록 설계한다.
- 대표 글과 목록 항목은 각각 전체 영역을 눌러 해당 글의 상세 화면으로 이동한다. 같은 글 ID로 연결하고, 뒤로 돌아왔을 때 홈의 목록 위치가 유지되는지 확인한다.
- 더미 글은 예시 콘텐츠임을 표시한다. 상세 화면의 본문 구성과 읽기 컨트롤은 별도 작업에서 정한다.

| 확인할 경우 | 표시·동작 기준 |
| --- | --- |
| 글이 없음 | 글 영역 대신 “새로운 이야기를 준비하고 있어요.”를 표시 |
| 글이 한 편 | 대표 글만 표시하고, 비어 있는 “더 읽어볼 이야기” 영역은 숨김 |
| 이미지 없음 | 이미지 공간을 생략하고 텍스트만으로 구성 |
| 이미지 표시 실패 | 제목·소개와 상세 이동은 계속 이용 가능하게 유지 |
| 긴 제목·글자 확대 | 텍스트가 겹치거나 고정 높이에 잘리지 않고, 각 글을 구분해 선택할 수 있는지 확인 |

이 구성은 홈의 정보 순서와 이동 동작에 대한 초안이다. 하단 탐색은 위 탭 디자인 초안을 따르며, 검색·분류·저장 버튼의 홈 배치 여부는 후속 시안에서 검토한다.

### 홈 시각 시안 01

- [브라우저용 HTML](./docs/prototypes/home-v1.html): 파일을 브라우저에서 열고 스크롤해 전체 구성을 확인한다. 별도 설치나 서버 실행은 필요하지 않다.
- [첫 화면 이미지](./docs/prototypes/home-v1.png): 390×844 CSS 픽셀 화면을 2배 해상도로 캡처했다.
- 상단의 별도 로고와 클로버를 생략하고 브랜드 문구의 ‘마실’에 물결체를 사용한다. 상단은 타이포그래피와 여백으로 구성하고, 대표 글의 예시 이미지는 기존 커피 일러스트를 사용한다.
- 상단 브랜드의 크기, 대표 글의 이미지·제목 비율, 나머지 글 목록으로 이어지는 여백을 검토한다. 화면의 문구와 글은 시안용 초안이다.
- 정적인 HTML·CSS 시안이며, 상태 표시줄도 표현용이다. 글 선택·상세 이동·하단 탭은 구현하지 않았다. 앱 구현·검증의 단계별 기준과 진행 상태는 [PLAN.md](./PLAN.md)에서 확인한다.

## 인터랙션과 검증

- 초기 홈 배경은 정적으로 검토할 수 있다. 느린 초록 배경 모션은 콘텐츠와 함께 확인한 뒤 적용 범위를 정한다.
- UI 피드백 150~240ms, 콘텐츠 전환 400~550ms는 원본의 성격을 이해하기 위한 참고값이다.
- 랜딩의 스크롤 문장 강조나 파트너 마키를 장문 읽기에 그대로 적용하지 않는다.
- 모션 감소 설정을 존중하고, 화면 밖이나 앱 비활성 상태의 불필요한 모션을 중단한다.
- 충분한 터치 영역, 안전 영역, 뒤로 가기, 화면 읽기 도구, 글자 확대를 확인한다.
- 긴 제목·긴 본문·이미지 없는 글·이미지가 늦게 표시되는 글을 사용해 iOS와 Android에서 확인한다.
- 네이티브와 WebView가 만나는 부분의 배경·서체·스크롤·로딩이 자연스러운지 확인한다.

현재는 문서 기준을 정리한 상태다. 앱 시안·실제 렌더·인터랙션 검증은 [PLAN.md](./PLAN.md)의 단계에서 진행한다.
