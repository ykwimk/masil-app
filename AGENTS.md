# Masil 작업 지침

마실은 에디터가 발행하는 콘텐츠를 독자가 가볍게 발견하고 편안하게 읽는 모바일 에디토리얼 앱이다.
이 프로젝트는 개발자가 React Native와 WebView 통신을 익히고 iOS·Android 출시까지 경험하는 것을 함께 목표로 한다.

## 문서 읽기

- [README.md](./README.md): 프로젝트 소개, 현재 상태와 개발 환경 실행.
- [PRODUCT.md](./PRODUCT.md): 제품 방향, 역할, 범위와 미정 사항.
- [DESIGN.md](./DESIGN.md): 랜딩에서 이어받는 브랜드 기준과 앱 디자인 제안.
- [PLAN.md](./PLAN.md): 개발 순서, 단계별 학습 목표·완료 기준과 진행 상태.

현재 사용자의 명시적인 요청을 우선한다. 제안·미정으로 표시된 항목을 확정 요구사항으로 취급하지 않는다.
결정이 바뀌면 해당 내용을 담당하는 문서를 갱신하고, 같은 설명을 여러 문서에 중복하지 않는다.
`PLAN.md`는 계획 중심 문서로 유지한다. 계획·범위·순서·완료 기준 또는 단계별 진행 상태가 달라질 때 갱신하며, 매 작업의 일지를 누적하지 않는다.

## 현재 작업 방향

- 제품·디자인·개발 방식의 문서 초안을 먼저 정리한다.
- 더미 데이터로 앱의 핵심 흐름을 개발하고, 콘텐츠 형식은 사용해보면서 수정한다.
- 앱의 핵심 읽기 흐름을 확인한 뒤 새 어드민과 서버를 만들고 연결한다.
- 기존 `masil/apps/web`은 참고 자료다. 기존 어드민을 재사용하거나 개조하는 것을 기본 계획으로 삼지 않는다.
- `masil/apps/landing`의 브랜드 기준을 앱에 맞게 적용한다. 랜딩의 섹션 구성과 웹 UI를 그대로 옮기지 않는다.
- 다른 저장소의 실제 변경은 해당 작업을 요청받았을 때 수행한다.

## 작업 단위와 진행 경계

- 한 턴에는 하나의 개념을 익히고 결과를 확인할 수 있는 최소 작업만 수행한다.
- 관련 파일을 여러 개 수정할 수 있지만, 서로 독립적인 작업을 묶지 않는다.
- 작업이 커지면 이번 턴의 범위를 줄이고 나머지는 다음 작업 후보로 남긴다.
- 안내한 범위의 구현과 검증에 필요한 수정까지 같은 턴에서 수행한다.
- 변경사항 검토, 스테이징, 커밋, 푸시는 사용자가 직접 한다.

## 학습을 포함한 개발 방식

개발자는 React 웹 프론트엔드 경험이 있고 앱 개발은 처음이다.

- 작업 전에 이번 목표와 생성·수정할 파일을 간단히 안내한다.
- 구현 전에 필요한 개념과 데이터 흐름을 짧게 설명한다.
- 웹에서 익숙한 개념과 모바일에서 달라지는 동작을 연결해서 설명한다.
- 학습 내용, 실제로 겪은 문제와 선택한 해결 방법은 해당 작업의 대화에서 설명한다.
- 별도의 학습 기록 문서는 사용자 요청이 있을 때만 작성한다.

## 구현 원칙

- 현재 Expo·React Native·Expo Router 구성을 출발점으로 사용한다. 설치 버전은 `package.json`과 lockfile에서 확인한다.
- API와 플랫폼 동작은 사용하는 버전의 공식 문서와 실제 기기 결과를 확인한다.
- 기존 코드·설치된 라이브러리·기본 플랫폼 기능을 먼저 확인하고 필요한 만큼 구현한다.
- 더미 데이터는 한곳에서 관리하고 고유 ID를 사용한다. 최종 DB 구조나 공통 데이터 계층을 미리 크게 만들지 않는다.
- 앱 화면 구성과 탐색은 모바일에 맞게 설계한다. WebView의 구체적인 사용 범위는 `PLAN.md`의 제안을 따른다.
- WebView 메시지와 외부 콘텐츠는 경계에서 검증한다. 허용한 메시지·탐색만 처리하고 비밀 키를 클라이언트에 넣지 않는다.
- 네트워크·저장 오류와 사용자 데이터 손실을 처리하는 동작을 생략하지 않는다.

## 검증과 보고

- 각 변경에 맞는 최소한의 검증을 수행한다. 문서 수정은 링크·내용·diff 검토로 확인한다.
- 화면과 인터랙션은 초기부터 iOS·Android에서 확인한다. 웹 미리보기만으로 모바일 검증을 대신하지 않는다.
- 안전 영역, 뒤로 가기, 글자 확대, 접근성, 모션 감소, 앱 재실행 등 변경과 관련된 조건을 확인한다.
- 코드 검증과 실제 화면·기기 검증을 구분한다. 확인하지 못한 플랫폼이나 동작은 명시한다.
- 구현 완료·검증 완료·사용자의 학습 확인을 구분해 보고한다.

작업이 끝나면 다음 내용을 대화에서 정리한다.

- 생성·수정한 파일 목록.
- 구현 내용과 코드 변경 이유.
- 실행한 검증과 확인하지 못한 사항.
- 실행 방법과 사용자가 직접 확인하거나 바꿔볼 포인트.
- 다음 작업 후보 1~2개.

보고 후에는 멈추고 사용자가 명시적으로 다음 작업을 지시할 때까지 기다린다.
다음 후보를 제시해도 자동으로 착수하거나 다음 우선순위를 연속으로 진행하지 않는다.
